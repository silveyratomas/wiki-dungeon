---
const { titulo, children } = Astro.props;
---

<div class="tarjetaNivel w-full max-w-5xl bg-black/80 border border-white/20 rounded-xl p-6 text-white shadow-md overflow-hidden relative">
  <h2 class="text-green-400 text-xl font-bold mb-4">{titulo}</h2>
  <div class="relative space-y-4 text-white text-base leading-relaxed">
    <slot />
  </div>
</div>
